{
  "name": "@web-auto/camo",
  "version": "0.3.5",
  "description": "Camoufox Browser CLI - Cross-platform browser automation",
  "type": "module",
  "bin": {
    "camo": "./bin/camo.mjs",
    "camo-bash": "./bin/camo"
  },
  "files": [
    "bin/",
    "scripts/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:file-size": "node scripts/check-file-size.mjs",
    "test": "node --test 'v2/tests/unit/**/*.test.mjs' && node --test 'v2/tests/smoke/**/*.test.mjs'",
    "test:v2": "node --test 'v2/tests/unit/**/*.test.mjs'",
    "test:smoke": "node --test 'v2/tests/smoke/**/*.test.mjs'",
    "gates": "cd v2 && node gates/run-all.mjs --strict",
    "version:bump": "node scripts/bump-version.mjs",
    "install:global": "npm run build && npm install -g .",
    "uninstall:global": "npm uninstall -g @web-auto/camo",
    "build:global": "npm run build && npm test && npm install -g ."
  },
  "keywords": [
    "browser",
    "automation",
    "camoufox",
    "cli",
    "playwright"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "camoufox": "^0.1.0"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "playwright": "^1.58.2",
    "pngjs": "^7.0.0",
    "ws": "^8.19.0"
  }
}
