// Storage paths. Module id=services.browser_service.internal.storage_paths.
//
// Compatibility projection. The profile service owns profile-data paths.

export {
    PROFILE_ID_PATTERN,
    resolveCookieBackupDir,
    resolveEphemeralTempDirName,
    resolveFingerprintPath,
    resolveProfileDir,
    resolveProfilesRoot,
} from '../../profile/storage_paths.mjs';
