interface:
  display_name: "Camoufox camo CLI"
  short_description: "Use only local camo CLI commands (browser + container + autoscript + events)"
  default_prompt: "Use $camoufox and only run camo CLI commands for profile/session lifecycle, browser actions, container subscriptions, autoscript orchestration, progress events, and cleanup diagnostics."

policy:
  allow_implicit_invocation: true
